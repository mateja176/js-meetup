type Mutation {
  createUser(
    name: String!
    lastname: String!
  ): User

  createNjam(
    location: String!
    description: String
    time: String!
    organizerId: ID!
  ): Njam

  orderNjam(
    njamId: ID!
  ): Njam

  deleteNjam(
    njamId: ID!
  ): Njam
}
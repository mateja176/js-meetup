overwrite: true
# schema: "./src/types/**/*.graphql"
schema: "http://localhost:4000"
generates:
  ./src/models.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
